# V1.5 Progressive Finance Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a progressive finance system that keeps fast daily bookkeeping simple while adding optional advanced modules.

**Architecture:** Extend the existing SQLite model with versioned migrations. Keep modules isolated behind enable flags and preferences. Preserve V1.4.1 data compatibility.

**Tech Stack:** Android Java, SQLite, existing custom UI components, JUnit tests, Gradle APK build.

## Global Constraints

- Preserve existing ledgers, accounts, balances and transactions.
- Keep local-first storage; no mandatory account login or cloud sync.
- Advanced modules must be independently enabled.
- Any operation affecting balances requires explicit confirmation.

---

### Task 1: Database V2 migration and module framework

**Files:**
- Modify: `LedgerDb.java`
- Create: `ModuleRepository.java`
- Create: `PreferenceRepository.java`
- Test: database migration tests

- [ ] Add migration tests for existing V1 database data preservation.
- [ ] Add tables for module switches and preferences.
- [ ] Implement module enable/disable queries.
- [ ] Verify old ledger and transaction records remain readable.

### Task 2: Fast bookkeeping workflow

**Files:**
- Modify: transaction UI components
- Create: quick bookkeeping state model

- [ ] Add recent category loading.
- [ ] Add default account and bookkeeper preferences.
- [ ] Add copy-last-transaction action.
- [ ] Verify expense entry can complete with minimal actions.

### Task 3: Budget module

**Files:**
- Create: budget data model
- Modify: statistics UI

- [ ] Add budget migration tables.
- [ ] Add monthly and category budget calculations.
- [ ] Add budget cards.
- [ ] Test over-budget calculations.

### Task 4: Templates, recurring bills and subscriptions

**Files:**
- Create: template models
- Create: recurring task models

- [ ] Add template creation and execution.
- [ ] Add recurring task confirmation flow.
- [ ] Add subscription reminder calculations.
- [ ] Verify no automatic balance changes without confirmation.

### Task 5: Statistics and data management

**Files:**
- Modify: statistics page
- Create: backup/export services

- [ ] Add trend calculations.
- [ ] Add CSV export.
- [ ] Add JSON backup and restore.
- [ ] Verify restore creates safety snapshot.

### Task 6: Privacy and release validation

**Files:**
- Create: security settings components
- Modify: settings page

- [ ] Add PIN protection storage using secure hashing.
- [ ] Add lock timeout behavior.
- [ ] Run full regression tests.
- [ ] Build and verify APK signature.
